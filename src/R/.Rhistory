sum(tmpdata[[k]]$V2)
length(tmpdata)
length(tmpdata[[j]]$V2)
mean(tmpdata[[j]]$V2)
Q
source("cooccur_links_bi.R")
stats_links
stats_links[["PRK-JPN"]]
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
linkname
Q
source("cooccur_links_bi.R")
linkname
j
k
tmpc[[j]]
tmpc[[k]]
Q
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
length(tmpdata)
length(tmpdata2)
Q
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
tmpdata
tmpdata[[j]]
j
tmpdata[[j]]$V2
mean(tmpdata[[j]]$V2)
mean(tmpdata[[k]]$V2)
n
c
source("cooccur_links_bi.R")
c
c
c
n
tmpdata[[j]]
j
length(tmpdata)
Q
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
a <- list()
a["dd"] = 1
a
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
a <- data.table()
a <- data.frame()
a
a
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
n
mdata
n
mdata
Q
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
mdata
n
mdata
n
mdata
Q
mdata
source("cooccur_links_bi.R")
mdata
tdata <- read.csv(filestr, header=FALSE, sep=",", stringsAsFactors=FALSE)
tdata
test <- merge(mdata, tdata)
test
test <- merge(mdata, tdata, all=TRUE)
test
length(all_countries)
Q
q()
getwd()
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
a <- c()
a[["aa"]] <- c()
a[["bb"]] <- c()
a
a[["aa"]][["cc"]] <- 0
a
a <- c(0)*15
a
a <- NULL
a <- c(a, 0)
a
a <- c(a, 0)
a
rep(0, 10)
a[["aa"]] <- rep(0, 10)
a
a[[1]] <- rep(0, 10)
a <- list()
a[["aa"]] <- rep(0, 10)
a[["bb"]] <- rep(0, 10)
a
names(a[["aa"]]) <- c("a", "b", "c", "d","e","f","g","h", "i", "j")
a
a[["aa"]]["b"]
source("cooccur_links_bi.R")
source("cooccur_links_bi.R")
stats_links
stats_links[["DEU-CHN"]]
stats_links[["GBR-CHN"]]
stats_links[["GBR-CHN"]][1]
stats_links[["GBR-CHN"]][2]
country_dist
for(i in 1:length(stats_names))
{
    tmpname <- stats_names[i]
    print(paste(tmpname, stats_counts[[tmpname]],"{",as.character(stats_links[[tmpname]]), "}", sep=" "))
    for(j in 1:length(stats_links[[tmpname]]))
    {
        for(k in 1:length(stats_links[[tmpname]]))
        {
            if(j == k)
                next
            country_dist[[subcountry]][subc2] <- country_dist[[subcountry]][subc2]+1
        }
    }
}
country_dist
for(i in 1:length(stats_names))
{
    tmpname <- stats_names[i]
    print(paste(tmpname, stats_counts[[tmpname]],"{",as.character(stats_links[[tmpname]]), "}", sep=" "))
    for(j in 1:length(stats_links[[tmpname]]))
    {
        for(k in 1:length(stats_links[[tmpname]]))
        {
            if(j == k)
                next
            country_dist[[stats_links[[tmpname]][j]]][stats_links[[tmpname]][k]] <- country_dist[[stats_links[[tmpname]][j]]][stats_links[[tmpname]][k]] + 1
        }
    }
}
for(i in 1:length(stats_names))
{
    tmpname <- stats_names[i]
    print(paste(tmpname, stats_counts[[tmpname]],"{",as.character(stats_links[[tmpname]]), "}", sep=" "))
    for(j in 1:length(stats_links[[tmpname]]))
    {
        for(k in 1:length(stats_links[[tmpname]]))
        {
            if(j == k)
                next
            country_dist[[j]][k] <- country_dist[[j]]][k] + 1
        }
    }
for(i in 1:length(stats_names))
{
    tmpname <- stats_names[i]
    print(paste(tmpname, stats_counts[[tmpname]],"{",as.character(stats_links[[tmpname]]), "}", sep=" "))
    for(j in 1:length(stats_links[[tmpname]]))
    {
        for(k in 1:length(stats_links[[tmpname]]))
        {
            if(j == k)
                next
            country_dist[[j]][k] <- country_dist[[j]]][k]+1
        }
    }
for(i in 1:length(stats_names))
{
    tmpname <- stats_names[i]
    print(paste(tmpname, stats_counts[tmpname],"{",as.character(stats_links[tmpname]), "}", sep=" "))
    for(j in 1:length(stats_links[tmpname]))
    {
        for(k in 1:length(stats_links[tmpname]))
        {
            if(j == k)
                next
            country_dist[[j]][k] <- country_dist[[j]]][k]+1
        }
    }
for(i in 1:length(stats_names))
{
    tmpname <- stats_names[i]
    print(paste(tmpname, stats_counts[tmpname],"{",as.character(stats_links[tmpname]), "}", sep=" "))
    for(j in 1:length(stats_links[tmpname]))
    {
        for(k in 1:length(stats_links[tmpname]))
        {
            if(j == k)
            {
                next
            }
            country_dist[[j]][k] <- country_dist[[j]][k]+1
        }
    }
}
country_dist
country_dist <- list()
length(stats_names)
stats_names[1]
stats_counts["CHN-FRA"]
stats_links["CHN-FRA"]
for(country in countries)
{
    country_dist[[country]] <- rep(0, length(countries))
    names(country_dist[[country]]) <- countries
}
for(i in 1:length(stats_names))
{
    tmpname <- stats_names[i]
    print(paste(tmpname, stats_counts[tmpname],"{",as.character(stats_links[tmpname]), "}", sep=" "))
    for(j in 1:length(stats_links[tmpname]))
    {
        for(k in 1:length(stats_links[tmpname]))
        {
            if(j == k)
            {
                next
            }
            if(stats_links[tmpname][j] %in% countries && stats_links[tmpname][k] %in% countries)
                country_dist[[stats_links[tmpname][j]]][stats_links[tmpname][k]] <- country_dist[[stats_links[tmpname][j]]][stats_links[tmpname][k]]+1
        }
    }
}
country_dist
tmpname
stats_links[tmpname]
length(stats_links[tmpname])
stats_links["ISR-PSE"]
stats_links["ISR-PSE"][1]
stats_links["ISR-PSE"][1][1]
stats_links["ISR-PSE"][1,1]
stats_links["ISR-PSE"][1,]
stats_links["ISR-PSE"][1]
stats_links["ISR-PSE"][[1]]
stats_links["ISR-PSE"][[1]][1]
stats_links["ISR-PSE"][[1]][2]
for(i in 1:length(stats_names))
{
    tmpname <- stats_names[i]
    print(paste(tmpname, stats_counts[tmpname],"{",as.character(stats_links[tmpname]), "}", sep=" "))
    for(j in 1:length(stats_links[tmpname]))
    {
        for(k in 1:length(stats_links[tmpname]))
        {
            if(j == k)
            {
                next
            }
            if(stats_links[tmpname][[1]][j] %in% countries && stats_links[tmpname][[1]][k] %in% countries)
                country_dist[[stats_links[tmpname][[1]][j]]][stats_links[tmpname][[1]][k]] <- country_dist[[stats_links[tmpname][[1]][j]]][stats_links[tmpname][[1]][k]]+1
        }
    }
}
country_dist
stats_links[tmpname][[1]][j]
stats_links[tmpname][[1]][1]
stats_links["ISR-PSE"][[1]][1]
stats_links["ISR-PSE"][[1]][1] %in% countries
stats_links["ISR-PSE"][[1]][1] %in% all_countries
for(i in 1:length(stats_names))
{
    tmpname <- stats_names[i]
    print(paste(tmpname, stats_counts[tmpname],"{",as.character(stats_links[tmpname]), "}", sep=" "))
    for(j in 1:length(stats_links[tmpname]))
    {
        for(k in 1:length(stats_links[tmpname]))
        {
            if(j == k)
            {
                next
            }
            if((stats_links[tmpname][[1]][j] %in% countries) && (stats_links[tmpname][[1]][k] %in% countries))
                country_dist[[stats_links[tmpname][[1]][j]]][stats_links[tmpname][[1]][k]] <- country_dist[[stats_links[tmpname][[1]][j]]][stats_links[tmpname][[1]][k]]+1
        }
    }
}
country_dist
country_dist[["EUR"]]
country_dist[["EUR"]][1]
country_dist[["EUR"]][1] <- country_dist[["EUR"]][1]+1
country_dist[["EUR"]][1]
country_dist[["EUR"]]["CHN"] <- country_dist[["EUR"]]["CHN"]+1
country_dist[["EUR"]][1]
country_dist <- list()
for(country in countries)
{
    country_dist[[country]] <- rep(0, length(countries))
    names(country_dist[[country]]) <- countries
}
for(i in 1:length(stats_names))
{
    tmpname <- stats_names[i]
    print(paste(tmpname, stats_counts[tmpname],"{",as.character(stats_links[tmpname]), "}", sep=" "))
    for(j in 1:length(stats_links[tmpname]))
    {
        for(k in 1:length(stats_links[tmpname]))
        {
            if(j == k)
            {
                next
            }
            name1 <- stats_links[tmpname][[1]][j]
            name2 <- stats_links[tmpname][[1]][k]
            if((name1 %in% countries) && (name2 %in% countries))
            {
                print(paste(name1, "||", name2, sep=""))
                country_dist[[name1]][name2] <- country_dist[[name1]][name2]+1
            }
        }
    }
}
for(i in 1:length(stats_names))
{
    tmpname <- stats_names[i]
    print(paste(tmpname, stats_counts[tmpname],"{",as.character(stats_links[tmpname]), "}", sep=" "))
    for(j in 1:length(stats_links[tmpname]))
    {
        for(k in 1:length(stats_links[tmpname]))
        {
            if(j == k)
            {
                next
            }
            name1 <- stats_links[tmpname][[1]][j]
            name2 <- stats_links[tmpname][[1]][k]
            if((name1 %in% countries) && (name2 %in% countries))
            {
                
                country_dist[[name1]][name2] <- country_dist[[name1]][name2]+1
            }
            else
            {
                print(paste(name1, " xx ", name2, sep=""))
            }
        }
    }
}
for(i in 1:length(stats_names))
{
    tmpname <- stats_names[i]
    print(paste(tmpname, stats_counts[tmpname],"{",as.character(stats_links[tmpname]), "}", sep=" "))
    for(j in 1:length(stats_links[tmpname][[1]]))
    {
        for(k in 1:length(stats_links[tmpname][[1]]))
        {
            if(j == k)
            {
                next
            }
            name1 <- stats_links[tmpname][[1]][j]
            name2 <- stats_links[tmpname][[1]][k]
            if((name1 %in% countries) && (name2 %in% countries))
            {
                
                country_dist[[name1]][name2] <- country_dist[[name1]][name2]+1
            }
        }
    }
}
for(i in 1:length(stats_names))
{
    tmpname <- stats_names[i]
    print(paste(tmpname, stats_counts[tmpname],"{",as.character(stats_links[tmpname]), "}", sep=" "))
    for(j in 1:length(stats_links[tmpname][[1]]))
    {
        for(k in 1:length(stats_links[tmpname][[1]]))
        {
            if(j == k)
            {
                next
            }
            name1 <- stats_links[tmpname][[1]][j]
            name2 <- stats_links[tmpname][[1]][k]
            if(!is.null(name1) && !is.null(name2) && (name1 %in% countries) && (name2 %in% countries))
            {
                
                country_dist[[name1]][name2] <- country_dist[[name1]][name2]+1
            }
        }
    }
}
for(i in 1:length(stats_names))
{
    tmpname <- stats_names[i]
    print(paste(tmpname, stats_counts[tmpname],"{",as.character(stats_links[tmpname]), "}", sep=" "))
    for(j in 1:length(stats_links[tmpname][[1]]))
    {
        for(k in 1:length(stats_links[tmpname][[1]]))
        {
            if(j == k)
            {
                next
            }
            name1 <- stats_links[tmpname][[1]][j]
            name2 <- stats_links[tmpname][[1]][k]
            if(is.null(name1) == FALSE && is.null(name2) ==FALSE && (name1 %in% countries) && (name2 %in% countries))
            {
                
                country_dist[[name1]][name2] <- country_dist[[name1]][name2]+1
            }
        }
    }
}
for(i in 1:length(stats_names))
{
    tmpname <- stats_names[i]
    print(paste(tmpname, stats_counts[tmpname],"{",as.character(stats_links[tmpname]), "}", sep=" "))
    for(j in 1:length(stats_links[tmpname][[1]]))
    {
        for(k in 1:length(stats_links[tmpname][[1]]))
        {
            if(j == k)
            {
                next
            }
            name1 <- stats_links[tmpname][[1]][j]
            name2 <- stats_links[tmpname][[1]][k]
            
            if(is.null(name1) || is.null(name2))
            {
                next
            }
            
            if((name1 %in% countries) && (name2 %in% countries))
            {
                
                country_dist[[name1]][name2] <- country_dist[[name1]][name2]+1
            }
        }
    }
}
country_dist
country_dist <- list()
for(country in countries)
{
    country_dist[[country]] <- rep(0, length(countries))
    names(country_dist[[country]]) <- countries
}
for(i in 1:length(stats_names))
{
    tmpname <- stats_names[i]
    print(paste(tmpname, stats_counts[tmpname],"{",as.character(stats_links[tmpname]), "}", sep=" "))
    for(j in 1:length(stats_links[tmpname][[1]]))
    {
        for(k in 1:length(stats_links[tmpname][[1]]))
        {
            if(j == k)
            {
                next
            }
            name1 <- stats_links[tmpname][[1]][j]
            name2 <- stats_links[tmpname][[1]][k]
            
            if(is.null(name1) || is.null(name2))
            {
                next
            }
            
            if((name1 %in% countries) && (name2 %in% countries))
            {
                
                country_dist[[name1]][name2] <- country_dist[[name1]][name2]+1
            }
        }
    }
}
country_dist
source("granger_kernel_compare.R")
source("granger_kernel_compare.R")
source("granger_kernel_compare.R")
source("granger_kernel_compare.R")
source("granger_kernel_compare.R")
source("granger_kernel_compare.R")
source("granger_kernel_compare.R")
source("granger_kernel_compare.R")
source("granger_kernel_compare.R")
source("cooccur_links_bi.R")
q()
